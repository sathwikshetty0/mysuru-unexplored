# Environment Variables Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ============================================
# OpenAI API Configuration
# ============================================
# Get your API key from: https://platform.openai.com/api-keys
# NOTE: In production, this should be set in your hosting provider's
# environment variables (e.g., Vercel), NOT in this file
VITE_OPENAI_API_KEY=your_openai_api_key_here

# ============================================
# Supabase Configuration
# ============================================
# Get these from: https://app.supabase.com → Project Settings → API
VITE_SUPABASE_URL=your_supabase_project_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ============================================
# IMPORTANT SECURITY NOTES:
# ============================================
# 1. The VITE_ prefix exposes these to the client-side code
# 2. NEVER put sensitive keys that should be server-side only here
# 3. For production deployment:
#    - Set OPENAI_API_KEY (without VITE_) in Vercel environment variables
#    - The serverless function will use it securely
# 4. Always enable Row Level Security (RLS) in Supabase
# 5. See SECURITY.md for complete security guidelines